import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dev_exam_movies_app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
